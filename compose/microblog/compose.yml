version: '3.7'

services:
  telegrambot:
    image: ghcr.io/sio/microblog-server
    user: ${USER}
    volumes:
      - type: bind
        source: {{ microblog_storage }}
        target: /storage
      - type: bind
        source: {{ microblog_storage_key }}
        target: /storage.key
