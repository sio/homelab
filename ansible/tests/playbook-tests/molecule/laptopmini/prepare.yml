- hosts: laptopmini
  become: yes
  tasks:
    - name: download vagrant default key
      delegate_to: localhost
      get_url:
        url: https://raw.githubusercontent.com/hashicorp/vagrant/master/keys/vagrant.pub
        dest:  '{{ lookup("env", "MOLECULE_EPHEMERAL_DIRECTORY") }}/vagrant-insecure.pub'
