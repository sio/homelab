# Clean environment for applying Ansible roles - with systemd inside
FROM debian:10-slim
RUN \
    apt-get update && \
    apt-get install -y \
        sudo \
        python3 \
        && \
    apt-get clean
RUN apt-get update && \
    apt-get install -y \
        systemd \
        && \
    apt-get clean
