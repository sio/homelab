all:
  children:
    hypervisor:
      hosts:
        hypervisor-j:
    lab:
      hosts:
        labaie:
        labtee:
        labxeo:
    offline:
      hosts:
        labaie:  # corrupted OS drive
    unreachable:  # do not delete this group even if it's empty
      hosts:
  hosts:
    ci-runners-manager:
    control:
    laptopmini:
    mediabox-v:
    morebooks.ml:
    playground:
