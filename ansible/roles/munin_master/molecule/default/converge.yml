---

- hosts: all
  become: true
  roles:
    - role: munin_master
      server_keys_public:
        - '{{ molecule_ephemeral_directory }}/testkey.pub'
      munin_master_nodes:
        - name: localhost
          address: 127.0.0.1

