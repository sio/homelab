---
scenario:
  name: default

driver:
  name: docker

platforms:
  - name: docker-debian-instance
    image: debian:buster
    # privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  log: true

verifier:
  name: testinfra
  directory: ../tests
  options:
    v: 1
