---

- hosts: all
  become: true
  roles:
    - role: gitlab_runner
      gitlab_runner_token: '{{ lookup("env", "GITLAB_RUNNER_TOKEN") }}'
      gitlab_runner_tags:
        - test
        - molecule
