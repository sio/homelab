---
# Personification
hlc:
  title: 'Книжная квартирка'
  cookie_key: 'RZQL51П*0027-/'
  id_key: '18251'

# HomeLibraryCatalog
url: morebooks.ml
server: apache2
server_config: '/etc/apache2/sites-enabled/{{ url }}.conf'
path:
  app:    '/home/{{ username }}/application'
  backup: '/home/{{ username }}/data/backup'
  data:   '/home/{{ username }}/data'
  static: '/home/{{ username }}/static'
  venv:   '/home/{{ username }}/virtualenv'
ssl:
  cert: '/etc/letsencrypt/live/{{ url }}/fullchain.pem'
  key:  '/etc/letsencrypt/live/{{ url }}/privkey.pem'
google_key: google684a6a967bc36817.html

# Paths to public keys on control machine
ssh_public_keys:
  - '~/.ssh/id_rsa.pub'
  - '~/.ssh/{{ url }}.pub'

# Operating system configuration
username: 'morebooks'
email_admin: 'sio.wtf@gmail.com'
locale: 'en_US.UTF-8'
charset: 'UTF-8'
timezone: 'Europe/Moscow'

# Dependencies
packages:
  - apache2
  - libapache2-mod-wsgi-py3
  - python-certbot-apache
  - python3-pip
  - python3-venv
  - python3
  - ufw
  - git
  - ntp
build_deps:
  - python3-lxml
  - python3-pil
