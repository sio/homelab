---

- hosts: all
  become: true
  vars_files:
    - ../../../../group_vars/all.yml
  vars:
    server_keys_public:
      - '{{ molecule_ephemeral_directory }}/testkey.pub'
    gerbera_media_dirs:
      - /hello/world/
  roles:
    - role: gerbera
