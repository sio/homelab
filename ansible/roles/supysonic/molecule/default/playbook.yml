---

- hosts: all
  become: true
  roles:
    - role: supysonic
      supysonic_music_dir: /music
      supysonic_url: url.to.supysonic
      server_keys_public:
        - '{{ molecule_ephemeral_directory }}/testkey.pub'
