---
dependencies:
  - role: require
    require:
      defined:
        - grub_kernel_params
      equals:
        ansible_os_family: Debian
        ansible_pkg_mgr: apt

galaxy_info:
  role_name: grub
  author: Vitaly Potyarkin
  description: Manage kernel parameters in Grub defaults
  license: Apache-2.0
  min_ansible_version: 2.0
  platforms:
    - name: Debian
      versions:
        - stretch
        - buster
