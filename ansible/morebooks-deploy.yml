---
tasks:
  - name: stop web server
    service:
      name: '{{ server }}'
      state: stopped

  - name: reinstall HomeLibraryCatalog
    pip: # unfinished

  - name: back up valueable data
    command: '{{ path.app|quote }}/backup.py'

  - name: start web server
    service:
      name: '{{ server }}'
      state: started

