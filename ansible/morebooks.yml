---
- hosts: all
  tasks:
    - name: remember unprivileged account used for remote access
      become: false
      set_fact:
        admin_unprivileged: '{{ ansible_user_id }}'
- hosts: all
  become: yes
  roles:
    - upgrade
    - motd
    - morebooks
