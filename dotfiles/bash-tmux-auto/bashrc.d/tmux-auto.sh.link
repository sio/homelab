if [[ $- == *i* ]]
then
    if [[ -z "$TMUX" && -z "$TMUX_PANE" ]]
    then
        echo 'Press Ctrl+C to cancel Tmux launch...'
        sleep 5 && {
            tmux attach || tmux new
            exit
        }
    fi
fi
