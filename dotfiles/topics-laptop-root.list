#
# REUSABLE CONFIG FILES FOR MY LAPTOP (system-wide configuration)
#
# Use `./bootstrap.sh topics.laptop-root` to put all config files in their places.
# Check individual topic directories to see what configuration will be applied.
#

#initramfs-kms-intel  # use Ansible role instead
#initramfs-zswap
keyboard
#modprobe-nouveau  # install nvidia-driver instead
xfce-flags
xorg-laptop-asus
